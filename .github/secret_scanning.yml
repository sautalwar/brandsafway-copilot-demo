# Secret scanning configuration for BrandSafway Safety Inspection Tracker
# Note: Secret scanning must also be enabled in repository Settings > Code security
# This file defines custom patterns for organization-specific secrets
paths-ignore:
  - "docs/**"
  - "*.md"
  - ".squad/**"
